<mvc:View controllerName="logaligroup.employees.controller.MainView"
    xmlns:mvc="sap.ui.core.mvc" displayBlock="true"
    xmlns="sap.m"
    xmlns:core="sap.ui.core">
    <Shell id="shell">
    <App id="app">
    <pages>
    <Page id="page" title="{i18n>title}">
        <content>
               <Panel id="p1">
               <Label id="labelEmployee" text="{i18n>labelEmployee}" class="sapUiSmallMargin" labelFor="inputEmployee"/>
               <Input id="inputEmployee" value="{/EmployeeId}" maxLength="10" width="200px" class="sapUiSmallMarginEnd"/>
               <Label id="labelCountry" text="{i18n>labelCountry}" class="sapUiSmallMargin" labelFor="slCountry" visible="true"/>
               <Select id="slCountry" selectedKey="{/CountryKey}" forceSelection="false" visible="true" items="{/ListCountry}">
               <items>
               <core:Item id="item1" key="{Key}" text="{Text}"/>
               </items>
               </Select>
               <Button id="btnFilter" text="{i18n>btnFilter}" icon="sap-icon://search" press="onFilter" class="sapUiSmallMarginEnd">
               </Button>
                <Button id="btnClearFilter" text="{i18n>btnClearFilter}" icon="sap-icon://clear-filter" press="onClearFilter" class="sapUiSmallMarginEnd">
               </Button>
               </Panel>
               <Panel id="p2">
                <Table id="tableEmployee" items="{/Employees}">
                <columns>
                <Column id="col1">
                <header>
                <Label id="label1" text="{i18n>tableId}"/>
                </header>
                </Column>
                 <Column id="col2">
                <header>
                <Label id="label2" text="{i18n>tableName}"/>
                </header>
                </Column>
                 <Column id="col3">
                <header>
                <Label id="label3" text="{i18n>tableCountry}"/>
                </header>
                </Column>
                 <Column id="col4">
                <header>
                <Label id="label4" text="{i18n>tableCity}"/>
                </header>
                </Column>
                </columns>
                <items>
                <ColumnListItem id="cli" type="Active" press="showPostalCode">
                <cells>
                 <Label id="lb1" text="{EmployeeID}"/>
                 <Label id="lb2" text="{LastName} , {FirstName}"/>
                 <Label id="lb3" text="{Country}"/>
                 <Label id="lb4" text="{City}"/>
                 </cells>
                </ColumnListItem>
                </items>
                </Table>
                 </Panel>
        </content>
    </Page>
    </pages>
    </App>
    </Shell>
</mvc:View>
