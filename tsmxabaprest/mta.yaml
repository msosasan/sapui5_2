_schema-version: "3.2"
ID: tsystems-tsmxabaprest
description: Consume REST ABAP service CRUD
version: 0.0.1
modules:
- name: tsystems-tsmxabaprest-destination-content
  type: com.sap.application.content
  requires:
  - name: tsystems-tsmxabaprest-destination-service
    parameters:
      content-target: true
  - name: tsystems-tsmxabaprest-repo-host
    parameters:
      service-key:
        name: tsystems-tsmxabaprest-repo-host-key
  - name: tsystems-tsmxabaprest-uaa
    parameters:
      service-key:
        name: tsystems-tsmxabaprest-uaa-key
  parameters:
    content:
      instance:
        destinations:
        - Name: tsystems-tsmxabaprest_html_repo_host
          ServiceInstanceName: tsystems-tsmxabaprest-html5-srv
          ServiceKeyName: tsystems-tsmxabaprest-repo-host-key
          sap.cloud.service: tsystems-tsmxabaprest
        - Authentication: OAuth2UserTokenExchange
          Name: tsystems-tsmxabaprest_uaa
          ServiceInstanceName: tsystems-tsmxabaprest-xsuaa-srv
          ServiceKeyName: tsystems-tsmxabaprest-uaa-key
          sap.cloud.service: tsystems-tsmxabaprest
          existing_destinations_policy: ignore
  build-parameters:
    no-source: true
- name: tsystems-tsmxabaprest-app-content
  type: com.sap.application.content
  path: .
  requires:
  - name: tsystems-tsmxabaprest-repo-host
    parameters:
      content-target: true
  build-parameters:
    build-result: resources
    requires:
    - artifacts:
      - tsystemstsmxabaprest.zip
      name: tsystemstsmxabaprest
      target-path: resources/
- name: tsystemstsmxabaprest
  type: html5
  path: .
  build-parameters:
    build-result: dist
    builder: custom
    commands:
    - npm install
    - npm run build:cf
    supported-platforms: []
resources:
- name: tsystems-tsmxabaprest-destination-service
  type: org.cloudfoundry.managed-service
  parameters:
    config:
      HTML5Runtime_enabled: true
      init_data:
        instance:
          destinations:
          - Authentication: NoAuthentication
            Name: ui5
            ProxyType: Internet
            Type: HTTP
            URL: https://ui5.sap.com
          existing_destinations_policy: update
      version: 1.0.0
    service: destination
    service-name: tsystems-tsmxabaprest-destination-service
    service-plan: lite
- name: tsystems-tsmxabaprest-uaa
  type: org.cloudfoundry.managed-service
  parameters:
    path: ./xs-security.json
    service: xsuaa
    service-name: tsystems-tsmxabaprest-xsuaa-srv
    service-plan: application
- name: tsystems-tsmxabaprest-repo-host
  type: org.cloudfoundry.managed-service
  parameters:
    service: html5-apps-repo
    service-name: tsystems-tsmxabaprest-html5-srv
    service-plan: app-host
parameters:
  deploy_mode: html5-repo
  enable-parallel-deployments: true
